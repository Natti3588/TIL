# 2026-07-15

## Tanstack Queryのキャッシュの仕組み

`staleTime`: 取得したデータを新鮮とみなす期間のこと。 この間はコンポーネントが再マウントされてもAPIに再リクエストはせずに、ユーザーのメモリに保存されたキャッシュをそのまま使う。

`gcTime`: useQueryが使われているコンポーネントがアンマウントされた際にカウントされ、
カウントが過ぎるとメモリに保存されたキャッシュは削除される期間

---



