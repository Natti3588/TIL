# 2026-08-31

## 学んだこと

- OpenAPIを書くことで得られるメリット
## わかったこと

フロントとバックで同じAPIを扱うときに、普通は同じ型を2回書く必要がありますが、
TypeSpecでOpenAPIを書いてそのOpenAPIからフロントとバックの型を生成してしまえば型を1回だけ書けばいいので楽ですし、型がズレないというメリットがあることが分かりました。

3層アーキテクチャの`domain`は独立させることが重要なので、外部ライブラリに依存しないのが良いことが分かりました。 例: `uuid`等



