# 2026-09-25

## 今日学んだこと

Go言語の`database/sql`

## わかったこと

今日はデータベースの設定のプロパティを書きたい。

`SetMaxOpenConns` 同時に開く接続数
`SetMaxIdleConns` 使い終わっても、閉じずに待機させる接続数
`SetConnMaxLifetime` 1音の接続を作り直す時間

`PingContext`をアプリケーション起動時に起動する理由は、DBが不具合で接続できなかった場合、
起動時にすぐにわかるため。
`pingContext`を行わないと最初のリクエストが来た時に漸く分かる。