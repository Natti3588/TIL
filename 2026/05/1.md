# 2026-04-26

## 学んだこと

- Zodバリデーション
- APIのエンドポイントとAPIラッパーの違い
- RESTful設計

## わかったこと

- 今迄スキーマとはデータベースのことだと思っていた（Prismaでデータベースを定義するときにスキーマを定義したから）けれどZodを学んでいくうちにスキーマとは**データ構造の設計図**であることが分かった。
- 本追加のAPIを作成するためにHonoのエンドポイントで（`POST /api/books/add`）を作成しようと考えていたんだけれど、それだとPOST（作成）と`/add`の意味が重複してしまうため（`POST /api/books`）が好ましい